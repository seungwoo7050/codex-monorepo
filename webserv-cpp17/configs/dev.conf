# webserv-cpp17 v0.4.0 예제 설정 파일 (Korean only 설명)
# - 포트와 최대 처리 요청 수를 인자로 전달하는 단순 실행 구성을 예시로 보여준다.
# - /health, /metrics 엔드포인트는 바이너리 기본 라우터에 내장되어 있으며 별도 설정 없이 동작한다.
# 사용법:
#   ./webserv 8080 5
#   curl http://127.0.0.1:8080/health
